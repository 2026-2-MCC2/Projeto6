import React from 'react';

const RegistrationRequests: React.FC = () => {
    return (
        <div>
            <h1>Registration Requests</h1>
            <p>Here you can view and accept registration requests from suppliers and organizers.</p>
            {/* Add logic to fetch and display registration requests */}
            {/* Add buttons to accept or reject requests */}
        </div>
    );
};

export default RegistrationRequests;